import { DAY, MINUTE } from 'maruyu-webcommons/commons/utils/time';

export const sessionKeepDuration = 30 * DAY;
export const userInfoKeepDuration = MINUTE;
export const authSessionKeepDuration = 5 * MINUTE;